<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:presentation="clr-namespace:TrainingTracker.Presentation;assembly=TrainingTracker.Presentation"
             x:Class="TrainingTracker.App.TrainingPlanPage"
             Title="Training Plan">

    <CollectionView ItemsSource="{Binding Weeks}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="presentation:WeekViewModel">
                <VerticalStackLayout Padding="16,8">

                    <Label Text="{Binding StartDate, StringFormat='Week of {0:d MMMM yyyy}'}"
                           FontAttributes="Bold"
                           FontSize="16"
                           Margin="0,8,0,4" />

                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding Days}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="presentation:DayViewModel">
                                <Grid ColumnDefinitions="110,*,80"
                                      Padding="0,3">
                                    <Label Grid.Column="0"
                                           Text="{Binding Date, StringFormat='{0:ddd d MMM}'}"
                                           VerticalOptions="Center" />
                                    <Label Grid.Column="1"
                                           Text="{Binding Session.Type}"
                                           VerticalOptions="Center" />
                                    <Label Grid.Column="2"
                                           Text="{Binding Session.DistanceKm, StringFormat='{0} km'}"
                                           HorizontalOptions="End"
                                           VerticalOptions="Center" />
                                </Grid>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1"
                             Color="LightGray"
                             Margin="0,8,0,0" />

                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
